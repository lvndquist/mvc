{% extends "base.html.twig" %}

{% block title %}Card Deck{% endblock %}

{% block body %}
    <main class = "small-margin">
        <div class = "deck-container">
            <div class = "playing-cards">
                {% for card in deck %}
                    <div class="deck-sprite card-{{card.toString()}}"></div>
                {% endfor %}
            </div>
        </div>
    </main>
{% endblock %}
